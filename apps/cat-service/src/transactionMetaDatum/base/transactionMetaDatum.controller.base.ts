/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { TransactionMetaDatumService } from "../transactionMetaDatum.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { TransactionMetaDatumCreateInput } from "./TransactionMetaDatumCreateInput";
import { TransactionMetaDatumWhereInput } from "./TransactionMetaDatumWhereInput";
import { TransactionMetaDatumWhereUniqueInput } from "./TransactionMetaDatumWhereUniqueInput";
import { TransactionMetaDatumFindManyArgs } from "./TransactionMetaDatumFindManyArgs";
import { TransactionMetaDatumUpdateInput } from "./TransactionMetaDatumUpdateInput";
import { TransactionMetaDatum } from "./TransactionMetaDatum";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class TransactionMetaDatumControllerBase {
  constructor(
    protected readonly service: TransactionMetaDatumService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: TransactionMetaDatum })
  @nestAccessControl.UseRoles({
    resource: "TransactionMetaDatum",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async create(
    @common.Body() data: TransactionMetaDatumCreateInput
  ): Promise<TransactionMetaDatum> {
    return await this.service.create({
      data: {
        ...data,

        pluralNameTest: data.pluralNameTest
          ? {
              connect: data.pluralNameTest,
            }
          : undefined,
      },
      select: {
        createdAt: true,
        id: true,

        pluralNameTest: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [TransactionMetaDatum] })
  @ApiNestedQuery(TransactionMetaDatumFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "TransactionMetaDatum",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findMany(
    @common.Req() request: Request
  ): Promise<TransactionMetaDatum[]> {
    const args = plainToClass(TransactionMetaDatumFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        createdAt: true,
        id: true,

        pluralNameTest: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: TransactionMetaDatum })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "TransactionMetaDatum",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findOne(
    @common.Param() params: TransactionMetaDatumWhereUniqueInput
  ): Promise<TransactionMetaDatum | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        createdAt: true,
        id: true,

        pluralNameTest: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: TransactionMetaDatum })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "TransactionMetaDatum",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async update(
    @common.Param() params: TransactionMetaDatumWhereUniqueInput,
    @common.Body() data: TransactionMetaDatumUpdateInput
  ): Promise<TransactionMetaDatum | null> {
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          pluralNameTest: data.pluralNameTest
            ? {
                connect: data.pluralNameTest,
              }
            : undefined,
        },
        select: {
          createdAt: true,
          id: true,

          pluralNameTest: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: TransactionMetaDatum })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "TransactionMetaDatum",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async delete(
    @common.Param() params: TransactionMetaDatumWhereUniqueInput
  ): Promise<TransactionMetaDatum | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          createdAt: true,
          id: true,

          pluralNameTest: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
